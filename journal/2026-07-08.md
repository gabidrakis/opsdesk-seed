# Journal · 2026-07-08 (J4 — Outils, MCP & orchestration)

[10:37] Setup J4 (Étape 1) terminé : `@modelcontextprotocol/sdk@^1.29.0` ajouté, import ESM/tsx vérifié (McpServer + StdioServerTransport). Baseline vitest : **vert** (17/17) avant et après install. Branche `j4-tools` depuis `etat/j3-fin`. Plan `plans/j4-tools-mcp.md` validé sous réserve d'ajustements labs.

[10:41] Lab 1 terminé (outil `classifier_ticket`) : `tools/classifier-ticket.mjs` (+ wrapper `run-classifier.mjs`, test 3 cas), slash-command `classer-ticket` réorientée vers délégation déterministe, note « contrat d'un outil » dans CLAUDE.md. Corps vide → `{erreur}` sans exception (exit 0 vérifié). Écart assumé : commande via `npx tsx` (pas `node` nu) car import `.ts`. Test : **vert** (vitest 20/20, dont 3/3 du lab).

[10:52] Lab 2 terminé (serveur MCP tickets) : `mcp/tickets-server.mjs` (3 outils gouvernés list/get/update, requêtes paramétrées, zéro SQL générique), `.mcp.json` versionné, `test/mcp-tickets.test.mjs` (Client MCP in-memory : 3 outils, id=999 géré, écriture réelle, injection rejetée). Boot sous `node` vérifié (initialize + tools/list). CLAUDE.md documenté. Test : **vert** (26/26). Reste checkpoint manuel Gabi (claude mcp list + 3 prompts + sqlite3).

[11:02] Checkpoint manuel Lab 2 fait (session Claude Code connectée au MCP) : `list_tickets{open}` → 9 tickets (cohérent avec `count(*)` SQLite hors-MCP), `get_ticket` présent/absent OK (`ticket 999 introuvable`), écriture réelle `update_ticket_status 1005 closed` vérifiée hors-MCP puis **restaurée** (`in_progress`), base revenue à l'état d'origine (9 open). CLI classifier Lab 1 rejoué OK. Test : **vert** (26/26). Note : `claude mcp list` affiche `tickets ⏸ Pending approval` en CLI fraîche — approbation au niveau session (outils déjà actifs ici) ; à approuver 1× au prochain `claude` interactif.

[11:52] Lab 4 terminé (pipeline planner→builder→reviewer sur `GET /tickets/stats`) sur `j4-orchestration`. 3 checkpoints humains tracés : **CP1** plan accepté (décisions A = fonction dédiée `computeTicketStats`, B = réponse stricte `{open,in_progress,closed}`) tracé dans `plans/stats.plan.md` ; **CP2** tests relancés par moi **33/33 vert** + `tsc` exit 0 + live `curl.exe /tickets/stats` = `{open:9,in_progress:2,closed:1}` cohérent avec SQLite (garde-fou respecté : pas de merge sur parole d'agent ; serveur zombie d'hier sur port 3000 tué au passage) ; **CP3** merge accepté → commit dédié `535cd01`. Reviewer verdict ACCEPTER + gate I3 reproduite (`reviews/stats.review.md`), aucun fichier modifié par lui. Agents custom non hot-loadés en session → rôles injectés dans `Plan` (planner, read-only) et `Explore`/`general-purpose` (reviewer/builder) — profils outils fidèles aux définitions. Capitalisé : workflow + route ajoutés à `CLAUDE.md`. Test : **vert** (33/33).

[11:18] Lab 3 terminé (spécifier planner/builder/reviewer) sur branche `j4-orchestration` (depuis `j4-tools`). 6 définitions versionnables : `.claude/agents/{planner,builder,reviewer}.md` (frontmatter name/description/tools + prompt de rôle + critère de sortie) et `.pi/agents/{planner,builder,reviewer}.md` (frontmatter minimal name/description, pour Lab 5 pi.dev). Non-chevauchement **garanti par les outils** : planner = lecture seule (Read/Grep/Glob), builder = seul avec Write/Edit (+Bash), reviewer = pas de Write/Edit (juge, ne code pas). Goulot humain validé par Gabi : **2 gates** (accepter le plan après planner · accepter le verdict après reviewer), builder→reviewer = gate auto I3 (vitest vert). Critères énoncé : `ls .claude/agents/` = 3 fichiers ✓, chacun rôle+outils restreints+critère de sortie ✓, relecture croisée sans chevauchement ✓. Pas de code applicatif (config d'agents) → vitest inchangé 26/26.
