# Journal · 2026-07-08 (J4 — Outils, MCP & orchestration)

[10:37] Setup J4 (Étape 1) terminé : `@modelcontextprotocol/sdk@^1.29.0` ajouté, import ESM/tsx vérifié (McpServer + StdioServerTransport). Baseline vitest : **vert** (17/17) avant et après install. Branche `j4-tools` depuis `etat/j3-fin`. Plan `plans/j4-tools-mcp.md` validé sous réserve d'ajustements labs.

[10:41] Lab 1 terminé (outil `classifier_ticket`) : `tools/classifier-ticket.mjs` (+ wrapper `run-classifier.mjs`, test 3 cas), slash-command `classer-ticket` réorientée vers délégation déterministe, note « contrat d'un outil » dans CLAUDE.md. Corps vide → `{erreur}` sans exception (exit 0 vérifié). Écart assumé : commande via `npx tsx` (pas `node` nu) car import `.ts`. Test : **vert** (vitest 20/20, dont 3/3 du lab).
