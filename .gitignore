node_modules
dist
data/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Journaux runtime de classify-batch (horodatés, non reproductibles)
journal/classify-batch-*.md

# Verdicts runtime de la revue agentique (horodatés par SHA, non reproductibles ;
# l'observabilité vit dans le Job Summary + le commentaire de PR — Décision A, J5)
reviews/revue-*.json
